import styled from "styled-components";

export const Container = styled.div`
  align-items: center;
  border: 1px solid #F39323;
  border-radius: 8px;
  display: flex;
  font-size: 1.4rem;
  font-weight: 400;
  flex-direction: column;
  justify-content: center;
  line-height: 2rem;
  margin: 26px 0 15px 30px;
  padding: 12px;
`

export const CansContainer = styled.div`
  display: flex;
  flex-direction: column;
`

export const InvalidValuesSpan = styled.span`
  color: #f4901d;
`
